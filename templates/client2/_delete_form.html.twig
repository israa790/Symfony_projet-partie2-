<form method="post" action="{{ path('client2_delete', {'id': client2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client2.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
